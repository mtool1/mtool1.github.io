const c=document.getElementById('app-content'); c.innerHTML='<span id="time">00:00:00</span><button id="start">Start</button><button id="stop">Stop</button><button id="reset">Reset</button>'; let sec=0; let id; function update(){sec++; const h=Math.floor(sec/3600); const m=Math.floor((sec%3600)/60); const s=sec%60; document.getElementById('time').textContent=[h,m,s].map(v=>v.toString().padStart(2,'0')).join(':');}
document.getElementById('start').addEventListener('click',()=>{ if(id)clearInterval(id); id=setInterval(update,1000);}); document.getElementById('stop').addEventListener('click',()=>clearInterval(id)); document.getElementById('reset').addEventListener('click',()=>{clearInterval(id); sec=0; document.getElementById('time').textContent='00:00:00';});