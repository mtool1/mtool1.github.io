const c=document.getElementById('app-content'); c.innerHTML='<input id="keywords" placeholder="keyword1 keyword2"/><button id="gen">Generate</button><p id="out"></p>'; document.getElementById('gen').addEventListener('click',()=>{const kw=document.getElementById('keywords').value.split(/s+/).filter(Boolean).map(k=>'#'+k.toLowerCase()); document.getElementById('out').textContent=kw.join(' ');});